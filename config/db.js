//initialise database
//dedicating a port number and telling our app.js to listen to that port.

/*
module.exports={
    database: 'mongodb://localhost:27017/ToDo_api_app',
    secret:'1234567890'
};
*/

/*
const MongoClient = require('mongodb').MongoClient;
const uri = "mongodb+srv://Anita:<password>@cluster0-yl0v1.mongodb.net/<dbname>?retryWrites=true&w=majority";
const client = new MongoClient(uri, { useNewUrlParser: true });
client.connect(err => {
  const collection = client.db("test").collection("devices");
  // perform actions on the collection object
  client.close();
});
*/
